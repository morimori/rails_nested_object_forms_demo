<li class="fields">
  <%= f.hidden_field :id %>
  <%= f.select :magic_id, options_for_select([['---', nil]] + Magic.all.map{|m| [m.name, m.id]}, f.object.magic_id) %>
  <%= link_to_remove_fields 'Remove Magic', f %>
</li>
